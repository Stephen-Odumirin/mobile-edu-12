<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.skool">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LectureDetailsActivity"></activity>
        <activity android:name=".authentication.StudentSignUpActivity" />
        <activity android:name=".authentication.LecturerSignUpActivity" />
        <activity android:name=".authentication.LecturerSignInActivity" />
        <activity android:name=".splash.AuthActivity" />
        <activity android:name=".authentication.StudentSignInActivity" />
        <activity
            android:name=".splash.SplashActivity"
            android:theme="@style/NoActionbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>